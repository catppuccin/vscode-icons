import type { BaseIcons } from '~/types'

export const baseIcons: BaseIcons = {
  file: '_file',
  folder: '_folder',
  folderExpanded: '_folder_open',
  // TODO create proper root folder
  rootFolder: '_folder',
  rootFolderExpanded: '_folder_open',
}
